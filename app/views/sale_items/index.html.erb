<p id="notice"><%= notice %></p>

<h1>Listing Sale Items</h1>
  <div class="row">
  <div class="col-xs-6">
    <% @sale_items.each do |sale_item| %>
      <%= render "item_row", sale_item: sale_item, order_item: @order_item %>
    <% end %>
  </div>
</div>

<br>

<%= link_to 'New Sale item', new_sale_item_path%>
