
<%= form_tag(sale_items_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Sale Items" %>
      <button class="btn" type="submit"><i class="icon-search"></i>Search</button>
    </div>

    Can't decide what to search for? See what others are buying in our public
    <%= link_to "order reports", { :controller => :order_items, :action => :display_order_report }%>.
 <% end %>

